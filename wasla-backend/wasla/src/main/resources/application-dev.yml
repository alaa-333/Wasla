# Dev profile — verbose logging, local Docker infra
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wasla
    username: wasla
    password: wasla123
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379 

  jpa:
    show-sql: true

logging:
  level:
    com.example.wasla: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
